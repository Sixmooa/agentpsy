# 网络请求故障排除指南

## 400错误解决方案

### 问题描述
当用户完成所有题目点击提交时，显示"网络请求失败400"错误。

### 问题原因分析
1. **API模式配置错误**：应用可能尝试连接真实Supabase API，但服务器期望的数据格式与发送的不匹配
2. **答案数据验证问题**：可能包含无效的答案数据（分数不在1-5范围内）
3. **网络连接问题**：无法连接到Supabase服务器

### 解决方案

#### 方案一：使用Mock API（推荐用于测试）
在开发阶段，可以使用Mock API来验证应用功能：

1. **修改NetworkModule.kt**：
   ```kotlin
   private const val USE_MOCK_API = true  // 设置为true
   ```

2. **Mock API特点**：
   - 完全离线工作，不需要网络连接
   - 提供模拟的MBTI测试结果
   - 返回完整的测试报告数据

#### 方案二：使用真实API
如果要连接真实的Supabase API：

1. **修改NetworkModule.kt**：
   ```kotlin
   private const val USE_MOCK_API = false  // 设置为false
   ```

2. **确保网络连接正常**：
   - 检查设备是否连接到互联网
   - 确认可以访问 `https://fmjcjcpfcosvukgkgliz.supabase.co`

3. **验证API密钥**：
   - 确保Supabase匿名密钥有效
   - 检查API权限配置

### 数据验证改进

应用现在包含以下验证机制：

1. **答案分数验证**：
   - 只接受1-5范围内的分数
   - 自动过滤无效答案

2. **问题ID验证**：
   - 确保问题ID大于0
   - 过滤无效的问题ID

3. **详细错误信息**：
   - 400错误：提示答案数据可能不完整
   - 网络错误：提供具体的网络问题描述
   - 服务器错误：区分不同类型的服务器问题

### 测试步骤

#### 使用Mock API测试
1. 确认 `USE_MOCK_API = true`
2. 重新编译应用
3. 完成测试题目
4. 点击提交，应该看到成功的结果页面

#### 使用真实API测试
1. 设置 `USE_MOCK_API = false`
2. 确保网络连接正常
3. 完成测试题目
4. 检查错误消息的详细信息

### 常见错误类型及解决方案

| 错误类型 | 错误消息 | 解决方案 |
|---------|---------|---------|
| 400错误 | "请求格式错误，可能是答案数据不完整" | 检查答案数量，确保所有题目都已回答 |
| 401/403错误 | "服务认证失败，请重新启动应用" | 检查API密钥，重启应用 |
| 网络错误 | "网络连接失败，请检查网络设置" | 检查网络连接，尝试切换网络 |
| 超时错误 | "网络超时，请检查网络连接后重试" | 检查网络速度，稍后重试 |
| 500错误 | "服务器内部错误，请稍后重试" | 等待一段时间后重试 |

### 开发者调试信息

如果需要进一步调试，可以：

1. **查看网络日志**：
   - NetworkModule中已启用详细的HTTP日志记录
   - 可以查看请求和响应的详细信息

2. **运行单元测试**：
   ```bash
   ./gradlew testDebugUnitTest --tests="*ApiRequestDebugTest*"
   ```

3. **检查数据格式**：
   - 使用 `ApiRequestDebugTest` 验证JSON格式
   - 确保请求体格式符合API期望

### 切换到生产环境

当应用准备发布时：

1. 设置 `USE_MOCK_API = false`
2. 移除或注释调试日志
3. 确保所有API端点正常工作
4. 进行完整的功能测试

---

**注意**：目前应用配置为使用Mock API (`USE_MOCK_API = true`)，这是为了避免400错误并确保功能正常。如果需要连接真实API，请按照上述指南进行配置。